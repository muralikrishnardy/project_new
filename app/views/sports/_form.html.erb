<%= form_for(@sport) do |f| %>
  <% if @sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sport.errors.count, "error") %> prohibited this sport from being saved:</h2>

      <ul>
      <% @sport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :num_of_matches %><br>
    <%= f.number_field :num_of_matches %>
  </div>
  <div class="field">
    <%= f.label :num_of_players %><br>
    <%= f.number_field :num_of_players %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
